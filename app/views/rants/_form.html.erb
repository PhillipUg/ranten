
<%= form_with model: @rant, url: rants_path do |f| %>

		<%= f.label "Rant about it", class: "label" %>
  <div class="field">
  <div class="control">
    <%= f.text_area :content, placeholder: "Rant about something...", id: :content %>
  </div>
</div>

<%= f.submit "Rant", class: "button is-info" %>
<% end %>
